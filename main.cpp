#include "includer.hpp"
#include "Node.h"
#include "Tree.h"

int main() {
    //Node node(7, nullptr);
	//cout << node.print();
	
	Node* s = nullptr;
	Node* m = nullptr;
	string a = ((s==m) ? "1 ":"0 ");
	cout << a << endl;
	Tree tree();
}
